# =============================================================================
# Python Dependencies - Video Watermark Removal Application
# =============================================================================

# --- Web Framework ---
fastapi==0.115.6
uvicorn[standard]==0.34.0

# --- File upload support for FastAPI ---
python-multipart==0.0.20

# --- Video processing (FFmpeg wrapper) ---
ffmpeg-python==0.2.0

# --- AI / Deep Learning ---
# CPU-only PyTorch (no CUDA). We have Intel Iris Xe hardware which is
# incompatible with NVIDIA CUDA. PyTorch CPU is the safest and most
# compatible option.
torch>=2.0.0
torchvision>=0.15.0

# --- ProPainter dependencies ---
# These mirror ProPainter's own requirements.txt, pinned for stability.
scipy>=1.10.0
einops>=0.6.0
imageio>=2.31.0
imageio-ffmpeg>=0.4.9
scikit-image>=0.21.0
av>=10.0.0
addict>=2.4.0
pyyaml>=6.0

# --- Image processing ---
opencv-python-headless==4.10.0.84
numpy>=1.24.0
Pillow>=10.0.0
